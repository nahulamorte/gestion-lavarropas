spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/gestion_lavadero
    username: usuario_dev
    password: password_dev

  jpa:
    hibernate:
      ddl-auto: update
    show-sql: true
    properties:
      hibernate:
        format_sql: true
